-- ✅ Custom UI (No library, fully original)

-- Services
local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local player = Players.LocalPlayer

-- Create UI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomHubUI"
ScreenGui.ResetOnSpawn = false
if syn and syn.protect_gui then
	syn.protect_gui(ScreenGui)
end
ScreenGui.Parent = game:GetService("CoreGui")

-- Main Frame
local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 500, 0, 300)
Main.Position = UDim2.new(0.5, -250, 0.5, -150)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

-- TopBar
local TopBar = Instance.new("TextLabel", Main)
TopBar.Size = UDim2.new(1, 0, 0, 30)
TopBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TopBar.Text = "Tutorial Hub | [3X] ⚡Clicker Simulator!"
TopBar.Font = Enum.Font.SourceSansBold
TopBar.TextColor3 = Color3.new(1, 1, 1)
TopBar.TextSize = 18

-- SideBar
local Side = Instance.new("Frame", Main)
Side.Size = UDim2.new(0, 130, 1, -30)
Side.Position = UDim2.new(0, 0, 0, 30)
Side.BackgroundColor3 = Color3.fromRGB(30, 30, 30)

-- AutoFarm Tab Button
local TabButton = Instance.new("TextButton", Side)
TabButton.Size = UDim2.new(1, 0, 0, 30)
TabButton.Position = UDim2.new(0, 0, 0, 10)
TabButton.Text = "● AutoFarm"
TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TabButton.TextColor3 = Color3.new(1, 1, 1)
TabButton.Font = Enum.Font.SourceSans
TabButton.TextSize = 16

-- Content Area
local Content = Instance.new("Frame", Main)
Content.Size = UDim2.new(1, -130, 1, -30)
Content.Position = UDim2.new(0, 130, 0, 30)
Content.BackgroundColor3 = Color3.fromRGB(20, 20, 20)

-- Rejoin Button
local RejoinButton = Instance.new("TextButton", Content)
RejoinButton.Size = UDim2.new(0, 150, 0, 40)
RejoinButton.Position = UDim2.new(0, 20, 0, 20)
RejoinButton.Text = "🔁 Rejoin Game"
RejoinButton.BackgroundColor3 = Color3.fromRGB(40, 100, 200)
RejoinButton.TextColor3 = Color3.new(1, 1, 1)
RejoinButton.Font = Enum.Font.SourceSansBold
RejoinButton.TextSize = 18
RejoinButton.BorderSizePixel = 0

-- Rejoin Function
RejoinButton.MouseButton1Click:Connect(function()
	TeleportService:Teleport(game.PlaceId, player)
end)
